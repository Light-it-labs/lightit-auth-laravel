# Passkeys (WebAuthn) frontend TODO

Generated by the light-it auth package. Delete this file once you've read it.

## Files written

| File | Contents |
| --- | --- |
| `src/services/auth/passkeys/types.ts` | `Passkey`, `PasskeyCreationOptions`, `PasskeyRequestOptions`, `PasskeyLoginResult`, `PasskeyCeremonyError`, and the `isPasskeySupported()` guard |
| `src/services/auth/passkeys/schemas.ts` | Zod schemas for a passkey's `name` (1-255 chars) and the `Passkey` list item |
| `src/services/auth/passkeys/api.ts` | One function per endpoint, the two `navigator.credentials` ceremony wrappers, and `mapPasskeyCeremonyError()` |
| `src/services/auth/passkeys/actions.ts` | `usePasskeys`, `useEnrolPasskey`, `useDeletePasskey`, `useSignInWithPasskey` |
| `src/routes/(public)/_guest/login/-components/passkey-login-button.tsx` | The sign-in button - hides itself when `isPasskeySupported()` is false |
| `src/routes/_private/security/-components/passkeys-section.tsx` | The management list - shows an explicit unsupported message rather than hiding, calls `usePasskeys`/`useDeletePasskey` |
| `src/routes/_private/security/-components/enrol-passkey-dialog.tsx` | The enrolment dialog - name prompt, then `useEnrolPasskey` |

**`src/routes/_private/security/page.tsx` was also generated, but not by this
installer**: the security page is shared with the two-factor account-management
screens, so a dedicated step composes it once every selected feature is known,
after Passkeys and 2FA have each written their own section component. It renders
`PasskeysSection` alone, `TwoFactorSection` alone, or both, depending on what you
selected.

## Wiring the screens in

None of the three files above patch an existing file - patching your customised
`login/page.tsx` or sidebar nav for three lines of JSX isn't worth the risk of a
bad match against however you've already changed them. Two small additions are
still yours to make:

- **Login page** - render the button, typically beside your password/Google
  form:

  ```tsx
  import { PasskeyLoginButton } from "./-components/passkey-login-button";
  // ...
  <PasskeyLoginButton />
  ```

- **Navigation** - link to `/security` from wherever your account menu or
  sidebar lives; nothing generated here does that for you.

**Route files were written.** Run `pnpm dev` (or `pnpm build`) once to
regenerate `src/routeTree.gen.ts` before `pnpm type-check` will pass. Until
then TanStack Router does not know these routes exist.

## Unsupported-browser story

`isPasskeySupported()` checks both `window.isSecureContext` and
`window.PublicKeyCredential`. This is deliberately stricter than a bare
`Boolean(window.PublicKeyCredential)` check: without the secure-context test, a
misconfigured dev origin (see below) fails with a raw `TypeError` instead of a
clean, handled "not supported" branch.

The two generated surfaces gate on it differently, on purpose:

- **Login button** - renders nothing when unsupported. There is nothing useful
  to say to someone who cannot use the feature on a screen that already offers
  password (and optionally Google SSO).
- **Passkey management section** - renders an explicit message instead of a
  disabled control, so a user who came looking for passkey settings learns why
  they are absent.

## Secure context and RP ID

WebAuthn only runs in a secure context:

| Dev URL | Works? | Why |
| --- | --- | --- |
| `http://localhost:5173` | Yes | `localhost` is a trustworthy origin by spec, even over plain HTTP |
| `http://127.0.0.1:5173` | Yes | Same rule |
| `https://myapp.test` (mkcert/Valet TLS) | Yes | Secure context |
| `http://myapp.test` (Valet/Herd default HTTP) | No | Not `localhost`, not HTTPS - `window.isSecureContext` is `false` |
| `http://192.168.1.20:5173` (LAN, phone testing) | No | Same as above - this is the one that bites, because testing a platform authenticator on a phone is exactly when you reach for a LAN IP |

Fix: terminate TLS locally (`mkcert`, or Herd/Valet's `secure` command) and set
`PASSKEY_RP_ID` accordingly.

**The RP-ID rule.** `PASSKEY_RP_ID` must be the origin's own host or a
registrable parent domain of it - never a sibling. If your frontend and API
live on different subdomains (`app.example.com` and `api.example.com`),
`PASSKEY_RP_ID` must be the shared parent (`example.com`). Getting this wrong
surfaces as a browser `SecurityError` with **no authenticator prompt at all** -
indistinguishable, from the user's side, from nothing happening.
`mapPasskeyCeremonyError()` surfaces `DOMException.name` verbatim for exactly
this reason: don't hide it behind a generic message. The generated enrolment
dialog branches on `.reason` rather than showing one generic message for every
failure; the login button does not - it shows one message for any failed
attempt.

## Interaction with two-factor authentication

If you've also generated the `two-factor-authentication` feature: a passkey
login returns a real `token_type: "Bearer"` and therefore **bypasses the
generated 2FA challenge pipeline**, which only runs inside the password login
flow. This is deliberate - the passkey stands in for the two-factor code
rather than adding to it - and is not something this package patches for you.
If you want both factors stacked instead, that is a change to your backend's
`LoginAction` pipe list, not to anything generated here.

## API contract

| Function | Path | Auth | Notes |
| --- | --- | --- | --- |
| `getPasskeyRegistrationOptions` | `{{ passkeyRegistrationOptionsEndpoint }}` | Bearer token | Feed the result straight into `createPasskeyCredential()` - its casing is the browser's own, never camelCased |
| `createPasskey` | `{{ passkeysEndpoint }}` (POST) | Bearer token | Completes enrollment; returns the new `Passkey` |
| `listPasskeys` | `{{ passkeysEndpoint }}` (GET) | Bearer token | Backs `usePasskeys` |
| `deletePasskey` | `{{ passkeysEndpoint }}/{id}` (DELETE) | Bearer token | Returns no content |
| `getPasskeyLoginOptions` | `{{ passkeyLoginOptionsEndpoint }}` | None | Public - this is how an unauthenticated visitor starts a passkey login |
| `loginWithPasskey` | `{{ passkeyLoginEndpoint }}` | None | Echoes back the `ceremonyId` from the options call |

No rename or update endpoint exists for an enrolled passkey - a name is set
once at creation. The generated management list does not offer one.

## Dependencies

Package manager detected: `{{ packageManager }}`

{{ dependencyReport }}

The generator never invokes a package manager. If a command is listed above,
run it yourself.
